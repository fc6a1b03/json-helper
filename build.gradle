//file:noinspection GroovyAssignabilityCheck
plugins {
    id "java"
    id "org.jetbrains.intellij.platform"
}

group "com.acme"
version "${gradle.ext.versions.ver}"

repositories {
    mavenCentral()
    intellijPlatform {
        defaultRepositories()
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

configurations.configureEach {
    resolutionStrategy {
        force "org.apache.commons:commons-lang3:${gradle.ext.versions.commonsLang3}"
    }
}

dependencies {
    /* BOM依赖 */
    implementation platform("cn.hutool:hutool-bom:${gradle.ext.versions.hutool}")
    implementation platform("tools.jackson:jackson-bom:${gradle.ext.versions.jackson}")
    /* IntelliJ 平台依赖 */
    intellijPlatform {
        intellijIdea(gradle.ext.versions.idea) {
            useInstaller = true
        }
        bundledPlugins([
                "org.toml.lang",
                "com.intellij.java",
                "com.intellij.gradle",
                "com.intellij.properties",
                "com.intellij.modules.json",
                "org.jetbrains.plugins.yaml"
        ])
    }
    /* 业务依赖 */
    implementation "cn.hutool:hutool-core"
    implementation "cn.hutool:hutool-http"
    implementation "tools.jackson.core:jackson-databind"
    implementation "tools.jackson.dataformat:jackson-dataformat-csv"
    implementation "tools.jackson.dataformat:jackson-dataformat-xml"
    implementation "tools.jackson.dataformat:jackson-dataformat-toml"
    implementation "tools.jackson.dataformat:jackson-dataformat-yaml"
    implementation "tools.jackson.dataformat:jackson-dataformat-properties"
    implementation "com.auth0:java-jwt:${gradle.ext.versions.auth0}"
    implementation "dev.toonformat:jtoon:${gradle.ext.versions.toon}"
    implementation "org.apache.poi:poi-ooxml:${gradle.ext.versions.apachePoi}"
    implementation "io.github.haibiiin:json-repair:${gradle.ext.versions.jsonRepair}"
    implementation "com.alibaba.fastjson2:fastjson2:${gradle.ext.versions.fastjson2}"
}

intellijPlatform {
    pluginConfiguration {
        id = "com.acme.json.helper"
        name = "Json Helper"
        version = project.version.toString()
        ideaVersion {
            sinceBuild = gradle.ext.versions.sinceBuild
        }
        vendor {
            name = "拒绝者"
            email = "asd1161575755@outlook.com"
            url = "https://github.com/fc6a1b03/json-helper"
        }
        description = """
            Json Helper is an IntelliJ IDEA plugin for editing, repairing, querying, and converting JSON,
            with Java model generation, tree navigation, structure copy, JWT and path parsing, plus export
            to XML, YAML, TOML, Properties, CSV, and XLSX.
        """.stripIndent().trim()
        changeNotes = """
            <ul>
                <li>Upgrade the build baseline to IntelliJ IDEA 2026.1, Java 25, and Gradle 9.4.1.</li>
                <li>Harden editor, search, dialog, and screenshot flows to reduce UI freezes and host-side impact.</li>
            </ul>
        """.stripIndent()
    }
}

runIde {
    javaLauncher = javaToolchains.launcherFor {
        languageVersion = JavaLanguageVersion.of(25)
    }
    jvmArgs += [
            "-Xms1g",
            "-Xmx4g",
            "-XX:+UseZGC",
            "-Xshare:off",
            "-Dfile.encoding=UTF-8",
            "-Duser.timezone=GMT+8",
            "-Didea.wsl.disable=true",
            "-XX:ReduceInitialCardSetAllocation=10",
            "--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED",
            "--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED",
            "--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED",
            "--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED"
    ]
    systemProperty "idea.ui.themes", "Darcula"
    systemProperty "file.encoding", "UTF-8"
    systemProperty "idea.encoding.charset", "UTF-8"
    systemProperty "idea.is.internal", "true"
    systemProperty "idea.auto.update", "false"
    systemProperty "idea.dynamic.classpath", "true"
    systemProperty "idea.accelerator.manager", "true"
    systemProperty "idea.cwm.station.enabled", "false"
    systemProperty "idea.inspections.enabled", "false"
    systemProperty "idea.plugins.developer.mode", "true"
    systemProperty "idea.plugins.developer.mode.debug", "true"
}

tasks.register("printVersion") {
    doLast {
        println project.version
    }
}

tasks.register("printJavaVersion") {
    doLast {
        println java.toolchain.languageVersion.get()
    }
}

tasks.register("printGradleVersion") {
    doLast {
        println gradle.gradleVersion
    }
}

tasks.register("printIdeaVersion") {
    doLast {
        println gradle.ext.versions.idea
    }
}

tasks.register("printAllVersions") {
    doLast {
        println "Project Version: ${project.version}"
        println "Platform Version: ${gradle.ext.versions.idea}"
        println "Java Version: ${java.toolchain.languageVersion.get()}"
        println "Gradle Version: ${gradle.gradleVersion}"
    }
}

tasks.register("cleanIntellijPlatformSandbox", Delete) {
    delete layout.projectDirectory.dir(".intellijPlatform/sandbox")
}

tasks.named("prepareSandbox").configure {
    dependsOn("cleanIntellijPlatformSandbox")
}

tasks.withType(JavaCompile).configureEach {
    options.release = 25
    options.encoding = "UTF-8"
    options.incremental = true
    options.compilerArgs += ["-Xdoclint:none"]
}
